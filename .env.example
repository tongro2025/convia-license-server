# === 공통 설정 ===
# 실행 환경: dev / prod
ENV=dev

# 라이선스 서버 공통 시크릿
PADDLE_WEBHOOK_SECRET=change_me_paddle_webhook_secret
ADMIN_API_KEY=change_me_admin_api_key
LICENSE_JWT_SECRET=change_me_license_jwt_secret

# === 개발용 (로컬) DB 설정 ===
# 로컬 개발 시 사용 (sqlite)
# 개발 환경에서는 편의상 sqlite 허용
DATABASE_URL=sqlite+aiosqlite:///./licenses.db

# === 운영용 (EC2/RDS) DB 설정 예시 ===
# 실제 운영에서는 아래처럼 PostgreSQL URL로 **직접** 설정해야 함
# 예시:
# DATABASE_URL=postgresql+psycopg2://USERNAME:PASSWORD@your-rds-endpoint:5432/DBNAME
